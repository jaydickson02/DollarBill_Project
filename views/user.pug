mixin cal(bills, dates)
  - var i = 0;
  ul
    for bill in bills
      li= bill + ' @ ' + dates[i]
      - i = i + 1;
      
      
      
html
  h1= h1
  +cal(billsL, datesL)
  
  form(method="post")
    input(name='bill' required) 
    input(name='date' required)
    input(type='submit')
